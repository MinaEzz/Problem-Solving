/**
 * Check if two values are strictly equal (same value & same type)
 * @param {*} a - first value
 * @param {*} b - second value
 * @returns {boolean}
 */

function strictComparison(a, b) {
  return a === b;
}

console.log(strictComparison(10, 10)); // true
